#!/usr/bin/env python3

# a program that prints ASCII alphabet in lowercase
# chr - a function used to convert ascii values to their corresponding characters.
for ch in range(97, 122):
    print(chr(ch),end="") 
